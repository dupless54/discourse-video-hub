name = "builder"
description = "T1 bounded Discourse Video Hub implementation."
model = "<BALANCED_CODING_MODEL>"
model_reasoning_effort = "medium"

developer_instructions = """
Follow AGENTS.md and docs/ai/EFFORT_ROUTER.md.
Keep scope bounded and preserve safety/validation rules.
No READY/self-approval state is required; merge eligibility follows the CI-only gate in AGENTS.md.
"""

name: Format Probe

on:
  pull_request:

jobs:
  format:
    runs-on: ubuntu-latest
    container: discourse/discourse_test:slim
    steps:
      - uses: actions/checkout@v6
      - name: Install JS dependencies
        run: pnpm install --frozen-lockfile
      - name: Show canonical Prettier output
        run: |
          pnpm prettier test/javascripts/integration/components/video-hub-watch-saved-test.gjs --write
          cat test/javascripts/integration/components/video-hub-watch-saved-test.gjs

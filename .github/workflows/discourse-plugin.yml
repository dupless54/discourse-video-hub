name: Format Probe

on:
  pull_request:

jobs:
  format:
    runs-on: ubuntu-latest
    container: discourse/discourse_test:slim
    steps:
      - uses: actions/checkout@v6
      - name: Install Ruby dependencies
        run: bundle install
      - name: Show canonical Syntax Tree output
        run: |
          echo '--- ranking_signals.rb ---'
          bundle exec stree format app/services/video_hub/ranking_signals.rb
          echo '--- ranking_signals_spec.rb ---'
          bundle exec stree format spec/services/video_hub/ranking_signals_spec.rb
